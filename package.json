{
  "name": "@app/root",
  "private": true,
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@11.22.0",
  "engines": {
    "node": ">=22",
    "pnpm": ">=11"
  },
  "scripts": {
    "check": "pnpm run lint && pnpm run deps:check && pnpm run typecheck && pnpm run test",
    "build": "pnpm -r --if-present build",
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "pnpm --filter @app/contracts test && pnpm --filter @app/shared test && pnpm --filter @app/patent-corpus test && pnpm --filter @app/ui test && pnpm --filter @app/notifier test && pnpm --filter @app/admin test:all && pnpm --filter @app/example_service test:all && pnpm --filter @app/patent_research test:all && pnpm --filter @app/glasses_management test:all && pnpm run test:scripts && pnpm run test:traceability",
    "deploy:preflight": "node scripts/deploy-preflight.mjs",
    "check:bindings": "node scripts/check-binding-ids.mjs",
    "db:migrate:env": "node scripts/d1-migrate.mjs",
    "test:scripts": "node --test scripts/lib/",
    "test:traceability": "node --test scripts/check-e2e-traceability.test.mjs && node scripts/check-e2e-traceability.mjs",
    "lint": "biome check .",
    "deps:check": "knip",
    "fix": "biome check --write .",
    "cf-typegen": "pnpm -r --if-present cf-typegen",
    "prepare": "lefthook install || true"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "knip": "6.32.2",
    "lefthook": "^2.1.10",
    "typescript": "catalog:"
  }
}
